name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  snake:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: prim21
          outputs: |
           outputs: |
           dist/github-contribution-grid-snake.svg?color_snake=39d353&color_dots=#0d1117,#0e4429,#006d32,#26a641,#39d353
           dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=00e5ff&color_dots=#0d1117,#0e4429,#006d32,#26a641,#39d353

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
